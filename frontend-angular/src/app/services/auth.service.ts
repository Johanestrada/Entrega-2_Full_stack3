import { Injectable } from '@angular/core'; import { HttpClient } from '@angular/common/http'; import { Observable,tap } from 'rxjs'; interface AuthResponse{token:string}
@Injectable({providedIn:'root'}) export class AuthService{private readonly key='coligo_session';constructor(private http:HttpClient){}login(username:string,password:string):Observable<AuthResponse>{return this.http.post<AuthResponse>('http://localhost:8084/auth/login',{username,password}).pipe(tap(r=>localStorage.setItem(this.key,r.token)))}isAuthenticated(){return !!localStorage.getItem(this.key)}logout(){localStorage.removeItem(this.key)}}
